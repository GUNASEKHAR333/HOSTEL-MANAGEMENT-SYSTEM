<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adina.godfhell">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/btn_login" />
        <!-- <activity android:name=".MainActivity" /> -->
        <activity
            android:name=".SignupActivity"
            android:label="@string/btn_sign_up"
            android:noHistory="true" />
        <activity
            android:name=".UserActivity"
            android:label="@string/user_profile" />
        <activity
            android:name=".NavigationHomeActivity"
            android:label="@string/title_activity_navigation_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainBarcodeActivity" />

        <meta-data
            android:name="com.google.firebase.ml.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".AfterMainBarcode" />
        <activity
            android:name=".Admin"
            android:label="Caterer"
            android:noHistory="true" />
        <activity
            android:name=".ForgotPassActivity"
            android:label="@string/btn_reset" />
        <activity android:name=".ProfileGoFragment" />
        <activity
            android:name=".DataIterateMainActivity"
            android:noHistory="true" />
        <activity
            android:name=".ParticularUser"
            android:noHistory="true" />
        <activity
            android:name=".change_price"
            android:noHistory="true" />
        <activity
            android:name=".MainImageUploadActivity"
            android:label="Choose Profile Image" />
        <activity android:name=".SecondHomeFragment" />
        <activity android:name=".UserListActivity"></activity>
    </application>

</manifest>